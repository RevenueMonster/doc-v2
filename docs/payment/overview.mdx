---
id: overview
title: Overview
sidebar_label: Overview
---

import { Heading, Text, Card, Image, Button } from "rebass";
import { Flex, Box } from "reflexbox";

## Integrate payments using RM APIs.

- #### Step 1 : Generate Access Token

Go to [Client Credentials](../quickstart/accesstoken/client-credentials) to get `Access Token`

- #### Step 2 : Start with a guide

<Flex flexWrap="wrap" mx={-2}>
  <Box p={2} width={[1, 1 / 3]}>
    <Box
      sx={{
        cursor: "pointer",
        p: 1,
        borderRadius: 2,
        border: "0.5px solid",
        borderColor: "#e3e8ee",
        transition: "0.3s",
        ":hover": {
          boxShadow: "0 8px 16px 0 rgba(0,0,0,0.2)",
        },
      }}
      onClick={() => {
        window.location.href = "././quick-pay";
      }}
    >
      <Text
        paddingTop={30}
        paddingBottom={30}
        fontSize="20px"
        color="background"
        bg="primary"
        textAlign="center"
        fontWeight="700"
      >
        Quick Pay
      </Text>
      <Text
        p={1}
        fontSize="12px"
        color="black"
        textAlign="left"
        paddingBottom={25}
      >
        <b> Merchant Scans User</b>
      </Text>
      <Text
        p={1}
        fontSize="12px"
        color="black"
        textAlign="left"
        paddingBottom={10}
      >
        Scan user eWallet QR code with RM terminal or RM Application.
      </Text>
    </Box>
  </Box>
  <Box p={2} width={[1, 1 / 3]}>
    <Box
      sx={{
        cursor: "pointer",
        p: 1,
        borderRadius: 2,
        border: "0.5px solid",
        borderColor: "#e3e8ee",
        transition: "0.3s",
        ":hover": {
          boxShadow: "0 8px 16px 0 rgba(0,0,0,0.2)",
        },
      }}
      onClick={() => {
        window.location.href = "././transactionQR/transaction-qr";
      }}
    >
      <Text
        paddingTop={30}
        paddingBottom={30}
        fontSize="20px"
        color="background"
        bg="primary"
        textAlign="center"
        fontWeight="700"
      >
        Transaction QR
      </Text>
      <Text
        p={1}
        fontSize="12px"
        color="black"
        textAlign="left"
        paddingBottom={25}
      >
        <b> User Scans Merchant </b>
      </Text>
      <Text
        p={1}
        fontSize="12px"
        color="black"
        textAlign="left"
        paddingBottom={10}
      >
        Merchant displays QR code for user to scan and pay.
      </Text>
    </Box>
  </Box>
  <Box p={2} width={[1, 1 / 3]}>
    <Box
      sx={{
        cursor: "pointer",
        p: 1,
        borderRadius: 2,
        border: "0.5px solid",
        borderColor: "#e3e8ee",
        transition: "0.3s",
        ":hover": {
          boxShadow: "0 8px 16px 0 rgba(0,0,0,0.2)",
        },
      }}
      onClick={() => {
        window.location.href = "././webpayment/web-payment";
      }}
    >
      <Text
        paddingTop={30}
        paddingBottom={30}
        fontSize="20px"
        color="background"
        bg="primary"
        textAlign="center"
        fontWeight="700"
      >
        Web Payment
      </Text>
      <Text p={1} fontSize="12px" color="black" textAlign="left">
        <b> Online acceptance eWallet and Credit card payment </b>
      </Text>
      <Text
        p={1}
        fontSize="12px"
        color="black"
        textAlign="left"
        paddingBottom={2}
      >
        Used by e-commerce and SaaS business platform.
      </Text>
    </Box>
  </Box>
</Flex>

### Payment Sequence Diagram

<Flex justifyContent="center">
  <Button
    width="100%"
    sx={{
      ":hover": {
        backgroundColor: "blue",
      },
      backgroundColor: "#528ef7",
      borderRadius: 15,
    }}
    onClick={() => {
      window.location.href =
        "https://drive.google.com/drive/folders/1nvDlHpGcu7cOD6Zv6bRfh6cBN9aIXO9Z?usp=sharing";
    }}
  >
    Our Payment Sequence Diagram
  </Button>
</Flex>

